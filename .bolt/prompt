For all designs I ask you to make, have them be beautiful, not cookie cutter. Make webpages that are fully featured and worthy for production.

By default, this template supports JSX syntax with Tailwind CSS classes, React hooks, and Lucide React for icons. Do not install other packages for UI themes, icons, etc unless absolutely necessary or I request them.

Use icons from lucide-react for logos.

HARD ARCHITECTURE CONSTRAINT — APPLIES TO EVERY PROJECT:
- Backend: Hono on Cloudflare Workers. NEVER Express, Fastify, or Koa.
- Database: Cloudflare D1 with Drizzle ORM (drizzle-orm/d1). NEVER PostgreSQL, pg, or node-postgres.
- Storage: Cloudflare R2. NEVER local filesystem or Bolt Database Storage.
- Auth: JWT or Cloudflare Access. NEVER passport, express-session, or connect-pg-simple.
- Async: Cloudflare Queues. NEVER bull or in-memory queues.
- Node.js built-ins (net, fs, http, crypto) are NOT available in Workers runtime. Do not import them.

If you find yourself reaching for Express or PostgreSQL, STOP. You are violating the architecture. Re-read this prompt.

Do NOT present "options" that include non-Cloudflare alternatives. Do NOT claim I confirmed a different architecture. Do NOT recommend "continuing with existing setup" if that setup violates these constraints. The answer is always Cloudflare-native.